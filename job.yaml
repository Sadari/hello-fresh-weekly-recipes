apiVersion: batch/v1
kind: Job
metadata:
  name: cicd-deployment
spec:
  template:      
    spec:
      containers:
      - name: hello-world
        image: gcr.io/omega-research-321011/hello-world
      restartPolicy: Never
  backoffLimit: 4
        
